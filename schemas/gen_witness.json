{
    "title": "Generate a witness",
    "required": ["circuit", "inputs"],
    "additionalProperties": false,
    "properties": {
        "circuit": { "type": "string" },
        "inputs": {
            "type": "object"
        }
    }
}
